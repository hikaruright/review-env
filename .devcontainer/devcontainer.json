{
  "name": "Re:VIEW Environment",
  "dockerComposeFile": "docker-compose.yml",
  "service": "review",
  "workspaceFolder": "/workspace",
  "shutdownAction": "none",
  "postCreateCommand": "docker --version",
  
  "runArgs": [
    
  ],

  "settings": { 
		"terminal.integrated.shell.linux": "/bin/bash"
	},

  "extentions":[
    "atsushieno.language-review"
  ]
}
